mixin dropdown(data={})
  .dropdown(
    class=data.className,
    class=(data.theme ? `dropdown--theme--${data.theme}` : null),
    class=(data.size ? `dropdown--size--${data.size}` : null),
    data-role=data.role,
  )
  